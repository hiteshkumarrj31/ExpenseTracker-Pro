{
  "name": "expensetracker-pro",
  "version": "1.0.0",
  "description": "A clean, offline-first personal finance tracker built with **HTML5, CSS3 and vanilla JavaScript** — no frameworks, no backend, no sign-up. All data lives in the browser's `localStorage`.",
  "main": "sw.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/core": "^8.4.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.6.8"
  }
}
